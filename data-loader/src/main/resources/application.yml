server:
  port: 8082
spring:
  application:
    name: data-loader
  profiles:
    active: local

cdc:
  kafka:
    input-topic: ${CDC_KAFKA_INPUT_TOPIC:cdc-transform-events}

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: always
